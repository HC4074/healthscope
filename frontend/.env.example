# Leave empty when the dashboard and API share an origin (the default).
# For a separately hosted API, provide its public origin without a trailing slash.
VITE_API_BASE_URL=

# Vite development proxy target; this value is not included in the browser bundle.
HEALTHSCOPE_API_PROXY_TARGET=http://localhost:8000
